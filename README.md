# Vue 3 + JavaScript + Vite

This project uses Vue 3 SFCs with plain JavaScript and Vite.

## Scripts

- `npm run dev`: start the Vite dev server
- `npm run build`: build the frontend
- `npm run preview`: preview the production build
